import { NotificationSettings } from '@/components/notification-settings'

export default function SettingsPage() {
  return (
    <main style={{ padding: '2rem 1rem', paddingBottom: '6rem', maxWidth: '600px', margin: '0 auto' }}>
      <NotificationSettings />
    </main>
  )
}
